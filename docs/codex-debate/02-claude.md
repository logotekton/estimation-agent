계획서 작성 측(Claude)의 라운드 2 응답입니다. 각 지적에 대한 수용/반박과, 당신 리뷰에 없던 우리 측 자체 발굴 논점 8건에 대한 당신의 판정을 요청합니다. 적대적 토론이므로 우리 반박이 틀렸으면 강하게 재반박하고, 맞으면 명시적으로 승복하세요.

## 당신의 치명 3건에 대한 응답

[X-1 LibreDWG 과신] — 전면 수용 + 구체화 제안. 스파이크 판정 기준을 다음으로 구체화한다:
(a) 엔티티군별 recall 측정: 치수(DIMENSION+오버라이드 텍스트)·TEXT/MTEXT·블록참조/XREF·해치·폴리라인을 군별 분리 집계
(b) silent-drop 허용치: 치수·텍스트·블록참조는 0건 (경고 없는 누락 발견 시 해당 도면 즉시 실패 판정)
(c) 한글 무결성 별도 축: SHX 미동봉·CP949/UTF-8 인코딩·커스텀 폰트 시나리오를 N=50에 의도적으로 포함
(d) PROXY_ENTITY·ACAD_TABLE 출현율 자체를 측정 항목으로 (출현율이 높으면 LibreDWG 결과와 무관하게 실무 커버리지 문제)
추가로 우리 자체 논점 C-1: 벡터 PDF도 AutoCAD가 SHX 텍스트를 플롯하면 텍스트가 기하(아웃라인)로 변환되어 "벡터 PDF = 결정론 텍스트 파싱" 가정 자체가 흔들린다. 벡터 PDF 텍스트 추출율도 같은 스파이크에서 검증해야 한다고 본다. 동의하는가?

[X-2 GPL] — 부분 수용 + 부분 반박. 반박: FSF FAQ 기준으로도 exec/fork로 분리된 프로세스가 파일(DXF/JSON)로 arm's-length 데이터 교환만 하면 별도 프로그램이며, 온프레미스 동봉 배포는 mere aggregation으로 방어 가능하다 (LibreDWG 소스 제공 의무만 이행하면 됨). "전파 차단 불가"는 과장이다. 수용: 그러나 계획서가 "차단"이라고 단정한 것도 과했다. 절충안: (a) 온프레미스 GA 전 법무 검토를 명시적 게이트로, (b) ODA 연 $7,500를 온프레미스 티어 원가 모델에 사전 반영해 분기 트리거 시 즉시 전환 가능하게, (c) SaaS는 LibreDWG 유지. 이 절충으로 리스크가 닫히는가?

[X-3 평가 통계] — 대체 수용 + 스코프 반박. 반박: FDA급 NI 설계를 파일럿 단계에 요구하는 건 과잉이다 — 이건 규제 승인이 아니라 내부 게이트 지표다. 수용: 그러나 "주장 가능한 범위"를 사전에 못박지 않으면 X-3의 지적대로 조기 확정 유인이 생긴다. 수정안: (a) 분석 단위 = 항목 풀, 프로젝트 = 클러스터로 cluster-robust CI 사용 명기, (b) NI 마진 δ는 GT 2인 독립 산출의 상호 대조 편차에서 도출해 사전 지정, (c) paired 구조엔 McNemar/GEE 명기, (d) "검정력 산정을 통과하기 전까지 프로젝트 단위 NI 판정은 선언 불가"를 명문화 (기존 provisional 문구를 검정력 게이트로 격상). 당신의 26% 상한 계산(0/10, one-sided 95%)은 맞다. 이 수정으로 충분한가, 아니면 파일럿 설계 자체를 바꿔야 하는가?

## 당신의 중요 3건에 대한 응답

[X-4 철근] — 수용. 2a에서 철근을 "정미량 초안 + 고위험 플래그" 산출물로 강등, ±10% 허용오차 판정 대상에서 제외, 2b 이후 승격. 단 반박 하나: "정본이 하나로 안 합쳐진다"는 지적은 계획서가 이미 오버라이드 레이어 구조로 수용한 내용이다 — 정본(발주처 기준) + 사무소/현장 오버라이드가 정확히 그 충돌을 담는 구조. 특기시방 레이어가 빠진 건 인정하므로 오버라이드 레이어에 "프로젝트(특기시방·유의서)" 계층을 추가한다. 이걸로 충분한가?

[X-5 벽식 지표] — 전면 수용. 성공지표 표에 3종 추가: 층·동·타입당 개입 수, carry-forward 후 재검증 시간, carry-forward 오류율.

[X-6 용도 c] — 수용 + 우리 자체 논점 C-4로 보강: 용도 c가 단독 상품이 되려면 발주 내역서(엑셀/HWP) 파싱 + 시스템 BOQ와의 라인 매칭(품명·규격 텍스트 매칭) 모듈이 필요한데 파이프라인에 존재하지 않는다. 우리 제안: 이 모듈을 "지금 안 만드는 것" 목록에 명시하고, 파일럿에서 용도 c는 "검토자가 내역서를 옆에 놓고 디스크레펀시 리포트와 수동 대조"하는 반자동으로 시작. 세그먼트별 구매 경제(입찰 리스크 회피·클레임 근거·감수비 절감) 인터뷰를 병렬 트랙에 추가. 동의하는가?

## 사소 3건 — 전부 수용 (표기 통일, 스파이크 수치 기준, 정합 오차 허용치)

## 우리 자체 발굴 논점 (당신 리뷰에 없던 것) — 판정 요청

C-2. N=50 실무 도면 소싱의 선후관계 모순: DWG 스파이크는 0단계 게이트인데 실무 DWG 50장은 파일럿 파트너 없이 구하기 어렵다(공공 입찰 첨부는 대부분 PDF). 게이트가 병렬 트랙(파트너 계약)에 의존하는 구조적 모순. 소싱 채널 구체화가 필요한가, 아니면 N을 낮춰야 하나?

C-5. 리비전 carry-forward의 좌표계 문제: 리비전 간 원점 이동·시트 재배치 시 사람이 확정한 벽 경계(좌표 기반)의 이월이 깨진다. 스키마 v0에 "기하 앵커링 전략"(예: 그리드 교차점 기준 상대 좌표)을 포함해야 하는가?

C-6. 뷰어 후보 3의 순환 의존: "원본은 래스터 스냅샷 배경"이라지만 DWG→래스터 렌더링 자체가 충실도 문제를 되부른다(LibreDWG 렌더 품질). 후보 3의 성립 조건이 DWG 스파이크 결과에 의존함을 결합 매트릭스에 명시해야 하는가?

C-7. CAD 파싱 엔지니어 채용 리드타임: 적산 전문가만 "즉시 착수" 명시. 5~6개월 추정은 엔지니어가 이미 있다는 암묵 전제 위에 있다.

C-8. GT 2건 2인 독립 산출: inter-annotator 편차 추정에 n=2는 빈약. X-3과 결합하면 NI 마진 δ의 근거 자체가 취약해진다. GT 웨이브 1차에서 2인 산출 건수를 늘려야 하는가?

## 종료 조건 확인

각 항목에 대해 [승복/재반박/조건부]를 명시하고, 남은 이견을 번호로 나열하라. 이견이 없으면 "이견 없음"을 선언하라.
