# 건설공사 견적 자동화 시스템 — 최종 계획서

> 이 문서는 초기 계획(v1)을 4라운드의 적대적 토론(독립 리뷰어 2인: 지속 토론 리뷰어 + 무이력 제3 리뷰어)을 거쳐 고도화한 최종본이다. 토론 이력 요약은 부록 A 참조.

---

## 0. 목표와 성공지표

**목표**: 도면 유래 공종(골조·마감·창호)의 물량을 검증 가능한 근거와 함께 산출하고, 도면 밖 공종(토공·흙막이·가설)은 파라미터 개산 모듈로 커버하여, 견적 검토 완료까지의 총 소요시간을 수작업 기준선 대비 **50% 이상 단축**한다.

### 성공지표
| 지표 | 정의 | 비고 |
|---|---|---|
| 물량 오차 | GT 매칭 항목의 오차 분포. **판정 단위는 공종 라인/부재 그룹 풀(pool) 수준** (GT 1건 = 수백 라인 표본). 총량 % 단독 지표 금지(누락·과다 상쇄 은폐) | 프로젝트 단위 통과 판정은 GT 10건+ 축적 후 |
| 항목 재현율/정밀도 | 누락 항목·유령 항목 검출 | 견적 사고의 주범은 물량 오차가 아니라 항목 누락 |
| 검토 완료 시간 | "검토 완료" = 영향액 상위 등급 전건 + [가정]·[불일치] 플래그 전건 확인 완료. 항목별 확인 행위는 타임스탬프와 함께 기록(§9 면책의 사실적 근거) | 목표 단축률 50% |
| **검토 완료 후 발견 오류율 (escaped defects)** | 파일럿 사무소의 사후 정산·감수에서 발견된 오류율. **단축률은 이 지표가 기준 이하일 때만 유효** — 플래그를 생성하는 것은 시스템 자신이므로, 페어 지표 없이는 "플래그 억제 → 검토 단축 달성"이라는 조작 유인이 내장됨. **기준은 절대치(0)가 아니라 "수작업 대비 비열등(non-inferiority)"** — 수작업에도 사후 오류는 존재하므로. 수작업 기준선 오류율은 GT 2인 독립 산출의 상호 대조에서 무비용 1차 추정 | 단축률의 페어 지표(필수) |
| 미검토 항목 사후 오류율 | 등급화로 인한 하위 항목 미검토가 품질 저하를 은폐하는지 감시 | |
| 반자동 개입 1회당 UX 소요시간 | 기준층 레버리지 논리의 성립 조건(개입이 10분이면 논리 붕괴) | 뷰어 스파이크 산출물 |

기준선 확보: GT 2인 독립 산출에 시간 기록 병행(무비용). 단 GT 산출은 감사용 고품질 산출이라 실무보다 느림 → **파일럿 사무소의 실무 산출 시간 기록으로 기준선 보정**.

> **주의(지연 관측)**: escaped-defect은 사후 정산·감수(검토 완료 후 수개월 뒤)에서 관측되는 지연 지표다. 따라서 파일럿 기간 중의 "단축률 50% 달성" 주장은 오류율 데이터가 성숙할 때까지 **잠정(provisional)** 으로 표기한다 — 내부 보고·투자 커뮤니케이션에서 지표가 조기 확정되는 것을 방지.

### 허용오차 (이원화)
| 공종 | 1차 목표 | 성숙 목표 |
|---|---|---|
| 콘크리트 | ±5% | ±3% |
| 거푸집 | ±7% | ±5% |
| 철근(이음·정착 반영) | ±10% | ±7% |
| 실별 마감 | ±10% | ±10% |

- GT 중 최소 2건은 2인 독립 산출로 inter-annotator 편차를 측정. 시스템 목표는 인간 편차보다 커야 함(인간 편차 미만 목표는 정의상 측정 불가).

---

## 1. 핵심 아키텍처 결정

**도면 파싱 → 경량 시맨틱 모델(3D 지오메트리 재구성 없는 부재 단위 구조화 데이터) → 룰 기반 물량산출.**

완전 자동 2D→3D BIM 재구성은 배제한다:
1. 연구 단계 난제이며, 도면 해석 오차 위에 모델링 오차가 누적되어 물량 오차의 원인 추적이 불가능해진다.
2. 물량 산출에 필요한 것은 3D 형상이 아니라 부재 속성(유형·규격·길이/면적·높이·공제)이다.

단순 파싱만으로도 부족하다: 물량은 부재 단위로 집계되고 공제·층고·마감 사양이 붙어야 하므로, 파싱 결과를 부재 객체(요소+속성+산출근거)로 승격해야 한다 — 사실상 "지오메트리 없는 BIM".

원칙:
1. **표(일람표·마감표·일반주기·층고표)가 도면 기하보다 정보량이 많다** → 표 구조화 우선
2. **수직 치원(층고·벽높이)은 층고표·단면도에서 파라미터로 취득** — 모델링 없이 3D 물량이 나오는 이유
3. **완전 자동이 아닌 "근거 달린 초안 + 사람 검증"** — 검토 UI가 제품의 본체
4. 누락 정보는 가정 엔진으로 채우되 가정 목록을 견적서에 명시
5. 결정론 파서 우선, VLM은 국소 사용

---

## 2. 타깃 고객과 사업 가설 (파일럿에서 검증)

**1차 고객(작업 가설): 중소·중견 시공사 견적팀.**
**용도 정의(중요)**: 한국 공공 입찰의 다수는 발주처가 물량내역서를 제공하는 **내역입찰**이라 시공사가 물량을 뽑지 않는다. 물량산출 수요가 실재하는 용도는:
- (a) **민간공사 견적** — 도면만 주고 견적을 요청하는 관행
- (b) **수주 후 실행예산·실행내역 작성**
- (c) **발주 내역 검증** — 제공된 내역의 누락·과소 물량 확인(디스크레펀시 리포트가 정확히 이 기능)

과금 가설: 프로젝트 건당(외주 적산비 대비 할인 포지션). 파일럿 계약에 유료 전환 가격·전환 트리거를 사전 합의로 명기.

**2차 세그먼트**: CM·발주처의 내역 검증 니즈 — 디스크레펀시 리포트 단독으로 앵커가 되는 시장.

**적산사무소는 고객이 아니라 파트너**: 검토 시간 단축이 공수 기반 수임료와 충돌(가치 제안 역방향). GT 생산·룰팩 감수의 공급자로 유인 설계(룰 기여 수익 공유 또는 우선 사용권).

**경쟁 구도**: 국내 적산 프로그램·단가 DB 사업자(물량은 사람이 입력)와 해외 AI takeoff(Togal.AI·Kreo — 한국 기준·내역 체계 미지원)의 사이. 차별선 = 한국 수량산출기준 룰팩 + 근거 추적성.

**리스크**: 시공사 세그먼트는 DWG 재출력 폴백 불가 세그먼트(원도면 재출력 채널·시간 없음)와 겹침 → DWG 변환 충실도가 **사업 가설의 성립 조건**. 스파이크 실패 시 분기(ODA 전환 비용 수용 / 세그먼트 재검토)를 §11 의사결정에 포함.

---

## 3. 입력 계층

| 입력 | 지원 | 처리 |
|---|---|---|
| DXF, 벡터 PDF | ◎ 1급 | 결정론적 벡터/텍스트 파싱 |
| DWG | ○ 변환 계층 | LibreDWG 우선(GPLv3 — SaaS 서버측 사용은 배포가 아니므로 카피레프트 미전파). **별도 프로세스(CLI) 격리**로 온프레미스 배포 시에도 전파 차단. 폴백: DXF/벡터PDF 재출력 요청 플로우 내장 |
| 스캔본 | △ 범위 제한 | 표·일람표·주기 구조화만 지원. 기하 추출 명시적 미지원 |

- **도면 세트 매니페스트**: 입력 단위는 단일 도면이 아니라 세트(수십~수백 장). 시트 자동 분류(구조평면/단면/일람표/마감표) + 층↔시트 매핑 UI + XREF 해결.
- **리비전 차분**: 변경도면 도착 시 재산출 + 차분 리포트. 사람이 확정한 사항(벽 경계, 레이어 매핑)의 이월(carry-forward) 정책을 스키마에 포함 — 시공사 입찰·민간 견적에서 리비전은 일상.
- **도면 품질 등급 판정기** 최전단 배치: 자동 / 반자동(레이어 매핑·경계 확정 지원) / 불가 즉시 판정.
- **스케일·단위 검증**: 기입 치수 vs 좌표 실측 대조. **자동 정정 금지** — CAD 치수 오버라이드가 존재하므로 어느 쪽이 참인지 기계 판정 불가, 디스크레펀시 리포트로 전달. 채택 우선순위(기입 치수 우선 관행)는 룰팩에 정의.

---

## 4. 파이프라인

```
입력 → [세트 매니페스트 → 품질 판정 → 스케일 검증]
 ① 기하 추출 (벡터, 결정론 파서)
 ② 시맨틱 추출
    - 텍스트 엔티티: 결정론 추출 / VLM은 표 구조 해석에만 국소 사용
    - 필수 구조화 대상: 부재 일람표, 마감표, 창호 일람표, 구조 일반주기, 층고표
    - 이중 모델 교차로 불일치 플래그
 ③ 경량 시맨틱 모델 + 교차검증
    - 평면 부재 카운트 ↔ 일람표 대조, 규격 분포 이상치 탐지
    - 층고 미검출 → 해당 수직 부재 물량 [가정] 플래그 강등
    - 디스크레펀시 리포트 (1급 출력물, §7 운영 정책)
 ④ 룰 엔진 (룰팩 구조)
    - 정본(source of truth) = 발주처 기준(조달청·LH·표준품셈), 사무소 관행은 오버라이드 레이어
    - 버전·발주처·사무소별 오버라이드 / BOQ 라인별 적용 룰 ID + 기준 조문 링크
    - 철근: KDS 14 20 52 이음·정착 기본값 + 일반주기 오버라이드, "정미량/반영량" 2라인 출력
 ⑤ BOQ 초안: 이산 출처 플래그 [직접추출|추론|가정|불일치발견]
    (수치 신뢰도 점수는 캘리브레이션 데이터 축적 후에만 — 미캘리브레이션 점수는 검토 스킵을 유도해 유해)
 ⑥ 검토 UI (제품의 본체, §6)
```

### 4-1. 중간 표현 스키마 (2a 착수 조건)

**부재 모델 스키마 v0** — 3계층 + 역추적 그래프:
1. **부재 인스턴스**: {ID, 기하 참조(시트·좌표), 시맨틱 속성, 출처 플래그, 층 귀속}
2. **물량 산식 트리**: 부재 → 산식(치수×층고−공제) → 적용 룰 ID
3. **BOQ 라인**: 공종분류 매핑 + 역추적(라인→산식→부재→도면 위치)

"원클릭 수정의 연관 항목 전파", 룰 조문 링크, 검토 완료 BOQ의 GT 환류, 리비전 carry-forward가 전부 이 스키마 위에서만 성립한다. 팀 병행 개발의 전제.

### 4-2. 골조 산출 전략 — 구조형식별 스코프

| 구조형식 | 시장 | 전략 |
|---|---|---|
| 라멘조 (기둥-보) | 오피스·상가·공장 | **2a단계.** 그리드 교차점 + 부재 태그(C1/G1) 카운팅. 태그 대표기입 관행("이하 동일") 대응: 그리드 위상 기반 미태그 부재 보간 로직 필수 |
| 벽식 (전단벽+슬래브) | 아파트·오피스텔 | **2b단계.** 태그 카운팅 불성립 인정 → 반자동 벽 확정 + **구간별 전개(균일 ×N 복제 금지)**: 지하층(외벽 두께 상이) / 필로티·전이층(전이보·전이슬래브 — 물량 밀도 상이, **누락 위험 최대 → 별도 확정 필수**) / 기준층 구간(일람표 층구간별 벽두께 감소 반영) / 최상층·옥탑. 성립 근거: 구조평면은 건축평면보다 깨끗(마감·심볼 없음, 벽 태그 존재) + 기준층 반복 레버리지(개입 1회당 물량 커버리지 최대) |
| 슬래브·기초 | 공통 | 카운팅이 아닌 면적 문제로 별도 취급: 그리드 스팬 근사 + 개구부(계단·EV) 공제는 반자동 경계 확정. **지하주차장 무량판 드롭패널은 가정이 아닌 태그 기반 산출** (해당 구조형식의 체계적 구성요소 + 물량 절대량 큼 + 태그·치수 명시 관례) |

- 드롭 슬래브·헌치·파라펫: 표준 가정 + [가정] 플래그(콘크리트 ±5% 내 흡수 가능), 상세 반영은 후속.

---

## 5. 도면 밖 공종 — 파라미터 개산 모듈

토공·흙막이·가설은 도면만으로 산출 불가함을 인정하고 파라미터 기반으로 커버: 사용자 입력(굴착깊이, 흙막이 공법, 부지 조건) + 용도별 표준 가정 → 파라메트릭 물량.

**BOQ 2부 구성 강제 분리**: 도면 유래부(허용오차 관리 대상) / 파라미터 개산부(**min~max 범위 출력** — 정밀도 착시 차단). 개산부 오류의 불신이 도면 유래부로 전염되는 것을 구조적으로 방지.

---

## 6. 검토 UI (뷰어 스파이크 대상)

기능: 항목↔도면 위치 하이라이트(기하 인덱싱), 원클릭 수정 + 연관 항목 전파, 반자동 경계 확정(스냅·폴리곤 편집), 레이어 매핑 지정, 미검토 출력물 워터마크(§9 연동).

**뷰어 의사결정 — DWG 변환 스파이크와 결합된 단일 매트릭스** (독립 결정이 아님: LibreDWG 실패로 ODA 전환 시 뷰어 바이의 경제성이 달라짐):
- 후보 1 (바이): 상용 뷰어 SDK — 단, APS(Forge)류 클라우드 전용은 온프레미스 요구와 충돌, ODA 계열은 ODA 비용 트리거와 결합
- 후보 2 (빌드): 원본 CAD 충실도 렌더러 자체 개발
- **후보 3 (빌드, 유력)**: **중간 표현(§4-1)을 자체 캔버스/WebGL로 렌더 + 원본은 래스터 스냅샷 배경**. 검토자가 봐야 하는 것은 근거 하이라이트 + 부재 폴리곤 편집이지 원본 DWG 완전 재현이 아니며, 이 경로는 DWG·벡터PDF·스캔본을 단일 렌더러로 통합한다
- 평가 축: 라이선스 비용, 온프레미스 호환성, 폴리곤 편집 확장성, **반자동 개입 1회당 UX 소요시간**(스파이크에서 측정)
- 결론은 스파이크가 내리되, **0단계 첫 2주 내 결론** — 2a UI 착수를 블로킹하지 않는 것이 일정의 선행 조건

---

## 7. 디스크레펀시 리포트 운영 정책

- 도면 간 불일치(평면↔일람표↔치수)는 오류가 아니라 **1급 출력물** — 내역 검증 세그먼트(§2 용도 c)에서는 단독 상품.
- **물량 영향액 기준 등급화·정렬**, 상위 N건 우선 노출 (전건 나열 시 alert fatigue로 성공지표인 검토 시간이 오히려 악화).
- 무시 패턴 학습은 **숨김이 아닌 접힘(collapsed)**: 강등 유형도 영향액 임계치 초과 시 재부상 + 강등 이력 감사 로그 (학습된 침묵이 실제 사고를 삼키는 것 방지).

---

## 8. 평가 체계

- **GT 웨이브** (순차 병목 방지): 1차 3~5건(그중 2건 2인 독립 산출 + 시간 기록)으로 2a 개발 병행 착수 → 파일럿 실 프로젝트의 "검토 완료 BOQ"를 사후 GT로 축적하는 플라이휠.
- 도면 소싱: 접근 권한이 명확한 공공 발주 설계도서(입찰 공고 첨부)부터.
- 허용오차 회귀 평가 CI화(항목 풀 수준 판정, §0).
- 파일럿 적산사무소 1~2곳 + **발주처 기준 준수를 제3 검증 채널로** (사무소 관행 과적합 방지).

---

## 9. 데이터·법무·책임

- 고객 도면 비밀유지·저작권: 모델 개선 활용은 계약 명시 동의 + 비식별화 파이프라인.
- 온프레미스 요구 대응: §3 프로세스 격리로 라이선스 리스크 사전 차단. SaaS 전용 여부는 사업 결정 사항으로 명문화.
- **산출 오류 책임 구조**: 약관 책임 한계(도구는 보조, 최종 서명은 검토자) + **검토 워크플로우의 항목별 확인 행위를 타임스탬프와 함께 기록하여 면책의 사실적 근거로 결합**(§0 검토 완료 정의와 동일 메커니즘) + 미검토 출력물 워터마크 + 손해배상 보험 검토. [가정] 플래그·min~max 출력을 면책 구조와 연결.
- VLM 비용: 결정론 파서 우선, 표·주기 영역 국소화.

---

## 10. 팀·기간

| 역량 | 인원 | 비고 |
|---|---|---|
| CAD 파싱/기하 엔지니어 | 1~2 | 2b(벽식) 진입 시 병목 예상 — 증원 트리거 |
| 풀스택 (뷰어·검토 UI) | 1~2 | 뷰어 스파이크 결과 따라 증감 |
| ML/VLM 엔지니어 | 1 | 표 구조화·시트 분류 |
| **적산 실무 경력자 (상근)** | 1+ | 룰팩 정본 작성·GT 감수. 표준품셈 연 개정 → 룰팩은 일회성 구축이 아닌 **지속 콘텐츠 옵스**(내부 유지 vs 파일럿 사무소 위탁을 §2 유인 설계와 연결). 채용 시장이 얇아 리드타임 김 — 즉시 착수 |

**기간**: 0단계 게이트(두 스파이크 판정) 4~6주 / 2a 첫 파일럿까지 **현실 추정 5~6개월** (3~4개월 달성은 뷰어 스파이크 2주 내 결론이라는 선행 조건 충족 시) / 2b·3단계는 2a의 인프라 재사용률 확인 후 추정 (지금 정밀 추정은 무의미).

---

## 11. 로드맵

### 첫 파일럿 최소 경로 (범위 비대화 방지 — 이것만으로 파일럿 1호)
**라멘조 2a + 층고표 파싱 + 검토 UI(핵심 기능만) + 디스크레펀시 리포트(등급화는 수동)**

### 지금 안 만드는 것 (명시적 후순위)
룰팩 오버라이드 UI(정본 하드코딩으로 시작) / 무시 패턴 학습 / 파라미터 개산 모듈 / 수치 신뢰도 / 민간 보정 계수 / MEP / 스캔본 기하 추출

### 단계
| 단계 | 내용 | 종료 조건 / 분기 |
|---|---|---|
| 0 (게이트) | **DWG 변환 스파이크 + 뷰어 빌드/바이 스파이크** (결합 매트릭스, §6) | DWG: 한국 실무 도면 N=50(ZWCAD·CADian 산출본 포함) 엔티티 보존율·텍스트/치수 무손실률 기준선 통과. **미달 시 분기: ODA 유료 전환 비용 수용 or 1차 세그먼트 재검토**(§2 — DWG 충실도는 사업 가설의 성립 조건). 뷰어: 첫 2주 내 결론 + 개입 1회 UX 시간 측정 |
| 병렬 트랙 (0단계와 무관하게 즉시 착수) | GT 1차 웨이브 발주 / 실무 도면 N=50 소싱 / 적산 전문가 채용 / 파일럿 파트너 계약 | 캘린더 타임이 긴 작업을 게이트에 담지 않음 |
| 1 | 공공데이터 기반 개산견적 (조달청 공사비정보광장·LH 공개 내역) | 출력에 데이터 출처(공공 실적)와 적용 한계 명시. 민간 보정 계수는 파일럿 데이터 축적 후 |
| 2a | 골조(라멘조) — **첫 파일럿** | 공종 라인 풀 수준 1차 허용오차 + 검토 시간(페어 지표 포함) 측정 |
| 2b | 골조(벽식): 반자동 벽 확정 + 구간별 전개 | 아파트 세그먼트 진입 |
| 3 | 마감: 반자동 실 경계 확정 + 마감표 매칭 | |
| 4 | 창호·잡철물 → MEP | |
| 병행 | 검토 UI는 2a부터 1급 과업 | |

---

## 12. 출력 규격

- **한국 내역 체계 매핑 = 1급 과업**: 공종분류(표준품셈 체계), 내역서 엑셀 포맷, 내역 프로그램 연동.
- IFC는 내부 부재 분류 명명 수준으로만 차용(비용 ~0, 공공 BIM 의무화 확대 대비 보험 — 2026년 500억+, 2028년 300억+ 공사 확대). **분류 체계의 정본은 한국 내역 매핑 측** (IFC 클래스↔한국 공종은 다대다 — 예: IfcCovering 하나가 미장·도장·수장으로 갈라짐).

---

## 부록 A. 토론 이력 요약 (4라운드)

| 라운드 | 주요 결과 |
|---|---|
| 1 | 리뷰어가 반론 14건 제기(치명 5). 핵심: DWG 라이선스 미검토, 실무 도면 벽선 추출 붕괴, 스캔본 파이프라인 부재, 토공·가설 공백으로 인한 목표 정의 오류, 철근 이음·정착 누락, VLM 환각, 신뢰도 점수 실체 없음, GT·정확도 목표 부재, 검토 UX가 제품 본체, 룰 코드화의 해석·버전 문제, 도면 간 불일치 정책 부재 |
| 2 | 12건 전면 수용 + 3건 반박(R1 GPLv3 SaaS 미전파, R2 태그 카운팅 강건성, R13 IFC 명명 유지). 리뷰어 R1·R13 승복, R2는 부분 재반박 — **"한국 주거는 벽식 구조라 태그 카운팅 불성립"** 유효타. 신규 7건: 층고 취득 경로 소실, GT 순차 병목, 개산부 분리, 불일치 alert fatigue, 파일럿 과적합, 공공데이터 대표성, 스파이크 종료 조건 부재 |
| 3 | 전건 수용(구조형식별 스코프, 구간별 전개 등) → 리뷰어 조건부 "이견 없음" 선언. 조건: 전이층 명시, 드롭패널 승격, 첫 파일럿 최소 경로, 무시 학습 안전장치 |
| 3.5 | 무이력 제3 리뷰어(엔지니어+PO) 투입 → 구조적 공백 8건: 뷰어 기술 계획, 사업 모델, 중간 표현 스키마, 도면 세트·리비전, 기준선 측정, 팀·기간, 책임 구조, GT 통계력 |
| 4 | 8건 반영 + 재량 결정 4건 판정 요청 → 리뷰어 보정 5건: **내역입찰 구조로 인한 용도 재정의**, 뷰어 제3 경로(중간 표현 렌더), 0단계 게이트 분리, 기준선 보정, **escaped-defects 페어 지표(플래그 억제 유인 차단)** — 최종 수용으로 양측 이견 없음 도달 |

### 토론으로 기각·수정된 초기 아이디어 (기록 보존)
- ~~"실제 공사에 근접한 견적서"~~ → 도면 유래 공종으로 목표 재정의 (토공·가설은 도면으로 산출 불가)
- ~~수치 신뢰도 점수~~ → 이산 출처 플래그 (미캘리브레이션 점수는 검토 스킵 유도로 유해)
- ~~"골조는 태그 카운팅으로 해결"~~ → 라멘조 한정, 벽식·슬래브는 별도 전략
- ~~"기준층 × N 전개"~~ → 구간별 전개 (전이층 별도 확정)
- ~~IFC 정렬 강조~~ → 한국 내역 체계 매핑이 1급, IFC는 명명 차용만
- ~~"검토 시간 단축" 단독 지표~~ → escaped-defects 페어 지표 필수
